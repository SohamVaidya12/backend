# Rename to .env and fill in values
DATABASE_URL=postgresql://<user>:<password>@<host>:<port>/<db>
BACKEND_SECRET=change-this-long-random-secret
# Optional: bind address and port
HOST=0.0.0.0
PORT=8000
